let indianAllycountries = ["russia", "japan", "france", "italy"];
let printfunc = (indianAllycountries) => {
    for (let i = 0; i < indianAllycountries.length; i++) {
        console.log(`${indianAllycountries[i]} is indian ally`);
    }
};
printfunc(indianAllycountries);
