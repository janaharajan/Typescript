let countryName: string = "INDIA"
let independanceYear: number = 1947;
let testww = function (countryName: string, independanceYear: number): void {
    console.log(`${countryName} and it got independance in the year of ${independanceYear}`);

}
testww(countryName, independanceYear);